# TEST_SPEC.md — go-particle-lab

<!-- scaffold template v1.30.0 から展開(2026-09-06) -->

## 実行規約

- `go test ./...` を stage 3–5 の判定に使用する。`-race` は cgo(64 bit gcc)が要り、この機では
  組めない(姉妹 go-parallel-web-crawler の loop_000 で実測)。本プロジェクトは goroutine を
  使わないので `-race` は要件にしない
- テストはネットワークに出ない・ファイルを書かない(N-04)
- `internal/` のテストはネイティブ(`windows/amd64`)で走る。`syscall/js` を import しない(N-02)ことで
  可能になっている
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す。前提を検算しない期待値は正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**。積分の期待値は SPEC §4.1 の式(半陰的オイラー)から導く |
| 外部権威(力学の保存則) | 運動量・運動エネルギー保存(e = 1)。出典をテストのコメントに書く |
| 実測 | **実測日と実測値**をコメントに残す(Lab Mode の表は画面に実測を出すだけで、テストは定数を持たない) |

件数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
乱数配置は seed を固定し、期待値は「別経路で同じ集合が出る」形で書く(G-02 / G-03)。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| 1 粒子・等加速度 | SPEC §4.1 の式 `v' = v + a·dt`、`x' = x + v'·dt` | 解析解 |
| 2 粒子の正面衝突(e = 1) | 運動量・運動エネルギー保存 | 外部権威 |
| 格子衝突 | 同じ配置に対する総当たり(O(n²))の衝突対集合 | 二経路一致 |
| 決定論 | 同じ世界を二度作り 600 ステップ進める | 二経路一致 |
| 変異体(G-06) | 隣接探索を 4 セルに削った格子 | 陽性対照 |

## ケース一覧

**「対応要求」には SPEC の ID を必ず書く**(HC-157)。

### L0(足場)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-30 | `go build ./...`(ネイティブ)が通る | exit 0 |
| T-002 | N-01 | `go.mod` に `require` が無い | 直接依存 0 |
| T-003 | N-02 | `internal/**/*.go` を走査 | `syscall/js` を import するファイルが 0 |

### L1(physics)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-101 | F-02 | 原本 §27: X=100, VX=10, dt=1, 加速度なし | X=110 |
| T-102 | F-02, F-03 | g=10, dt=0.5, 初速 0 を 1 ステップ | VY=5、Y = y₀ + 5×0.5 = y₀+2.5(§4.1 の式) |
| T-103 | F-04 | wind=4, dt=0.25 | VX=1 |
| T-104 | F-05, G-04 | 右壁へ VX=100 で突入(e=0.5)。ランダム 500 粒子を 200 ステップ | 反射後 VX=−50・X = W−r。全粒子が [r, W−r]×[r, H−r] 内 |
| T-105 | F-05 | 反発係数 e=0 で壁に当たる | 法線速度 0、接線速度は保存 |
| T-106 | F-06, G-01 | 質量 1・1 の正面衝突(e=1)、質量 1・3 の斜め衝突(e=1) | 運動量ベクトル一致(1e-9)、運動エネルギー相対誤差 < 1e-9。離れつつある対は速度不変 |
| T-107 | F-07, G-02, G-06 | 200 粒子 × 20 seed。格子と総当たりの衝突対集合 | 集合一致。**変異体(4 セル)は少なくとも 1 seed で不一致** |
| T-108 | F-12, G-03 | 同 seed・同入力列・600 ステップを二度 | 全位置・全速度が `==` で一致 |
| T-109 | F-08 | 井戸 (0,0)・Strength S・粒子が距離 d=100 に静止 | 1 ステップ後の速度が井戸向きで大きさ `S/d²·dt`。Radius 外の粒子は不変。d < dMin で発散しない |
| T-110 | F-09 | 矩形 [100,200]×[100,200] に左から進入 | X = 100−r・VX 反転(×e)。VY 不変 |
| T-111 | F-10 | 移動壁 A=50, ω=1 | t=0 で X0、t=π/2 で X0+50 |
| T-112 | F-11 | ブラックホール EventRadius=10、距離 5 の粒子 | 1 ステップで世界から除かれる。距離 100 の粒子は残る |
| T-113 | F-08 | 井戸 Capture=50・Damping=2・Strength=0、Capture 内の粒子 VX=100、dt=0.1 | VX=80。Capture 外は 100 のまま |

### L2(game)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-201 | F-20 | N=500, seed 固定で開始 | 粒子数 500・全て世界内・初速 0 |
| T-202 | F-21 | Goal 内に粒子を置いて 1 ステップ | 到達数 +1・粒子数 −1 |
| T-203 | F-23 | 到達を 0.5 秒間隔で 3 回、その後 1.5 秒空けて 1 回 | 得点 10+20+30、コンボ 3 → 途切れて 1、4 回目は +10 |
| T-204 | F-23 | 到達を 0.5 秒間隔で 12 回 | 倍率は 8 で頭打ち(9 回目以降 +80) |
| T-205 | F-22, F-24 | 目標 100 到達で残り 23.7 秒 | status=clear・タイムボーナス 230 |
| T-206 | F-22 | 60 秒経過 | status=timeup・以後 step しても状態不変 |
| T-207 | F-25 | 上限 1 の井戸に 2 度 add | 井戸は 1 個。move/remove は index 指定で効く |
| T-208 | F-26 | 10 ステップ後 Reset | T-201 と同じ初期状態(位置 bit 一致) |
| T-209 | F-27 | ステージ 1〜7 を全て生成 | 表 §5 の g / 風 / 障害物数 / 井戸上限 / ブラックホール有無と一致 |
| T-210 | F-26 | Pause 中に step | 時間もスコアも位置も不変 |
| T-211 | G-07 | 自動プレイ(定点 6 秒集め → 経由点 → Goal 2 秒滞在、周期 14〜16 秒)を全 7 ステージ × 3 seed | すべて status=clear(60 秒以内) |
| T-212 | G-07 | 陰性対照: 井戸なしの Stage 1 を 60 秒 | 到達 0・timeup |

### L3(wasm / web)

Go 側の `cmd/wasm` は `syscall/js` を使うためネイティブで `go test` できない。
`GOOS=js GOARCH=wasm go build` が通ること(G-05)と、実ブラウザ検品で確かめる。
実ブラウザ検品は `node scripts/check-browser.mjs [--url <本番>]`(Playwright はフリートの
`gihitsu-kobo/node_modules` から借りる)。**バナーやラベルのはみ出しは状態を読む検査では緑のまま通る**ので、
`getComputedStyle` と `getBoundingClientRect` の実測で見る(2026-09-07 に 2 度踏んだ)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-301 | F-30, G-05 | `GOOS=js GOARCH=wasm go build -o web/main.wasm ./cmd/wasm` | exit 0 |
| T-302 | N-03 | `web/app.js` を grep | `+= .*dt`・`Math.hypot`・`sqrt` が無い(運動則を持たない) |
| T-303 | F-32 | 実ブラウザ: 500 粒子で 1 秒間描画 | 位置配列の長さ = 2×粒子数。FPS 表示が更新される |
| T-304 | F-43 | 実ブラウザ: クリック → 井戸が出る、右クリック → 消える | 状態 JSON の wells 長が 1 → 0 |
| T-305 | F-45 | Lab Mode 実行 | 5 行の表に ms が入る(実測値・定数期待なし) |
| T-306 | F-32 (HC-190) | 状態 JSON の契約: `bridge.Marshal` の全キーが小文字始まり、`app.js` が `s./w./o./g./b.` で読むキーがすべて存在する | 一致(入れ子の wells/obstacles/goal/blackHole を含む) |
| T-307 | F-43, G-07 | 実ブラウザ: 井戸を床に置き 6 秒待って Goal へドラッグ、2 秒滞在 | 到達 30 個以上・得点が入る |
| T-308 | F-40, F-43 | 狭い画面(iPhone 13 相当 390px): 横スクロール・タッチのドラッグ運搬・CLEAR バナーの矩形 | `scrollWidth ≤ clientWidth`・到達 30 個以上・バナーの副文がキャンバスの左右に収まり `scrollWidth ≤ clientWidth` |
