# SPEC.md — go-particle-lab

<!-- scaffold template v1.30.0 から展開(2026-09-06)。以後このファイルはプロジェクトが育てる -->

原本の構想は [docs/go-particle-lab-spec.md](docs/go-particle-lab-spec.md)(31 節)。
本書はそれを**要求 ID 付きに写し、実測で決まった値と設計判断を上書きしていく**正本である。
原本と本書が食い違うときは本書が勝つ(食い違いは §7 に理由つきで残す)。

## 1. 目的

ブラウザ上に数百〜数千の粒子を生成し、重力・壁との反発・粒子同士の衝突・風・重力井戸を
リアルタイムに計算する。**物理演算はすべて Go で書き、WebAssembly としてブラウザで走らせる。**
JavaScript は描画・入力・UI だけを担い、粒子の運動則を一切知らない。

利用者はクリックで重力井戸(Gravity Well)を置き、落ちてくる粒子を画面右の Goal へ導く。
制限時間内に規定数を集めればクリア。粒子数を変えて物理時間の変化を見る Lab Mode を併設する。

キャッチコピー: **Go Particle Lab — Build gravity. Bend particles. Break physics.**

## 2. 機能要求

### 2.1 物理エンジン(`internal/physics`)

| ID | 要求 | 優先度 | 原本 |
|---|---|---|---|
| F-01 | 粒子は位置 (X,Y)・速度 (VX,VY)・半径・質量を持つ(`Particle`) | must | §5 |
| F-02 | 半陰的オイラー法: `v += a·dt` の後 `p += v·dt`。dt は秒 | must | §6 |
| F-03 | 一様重力: 加速度 `(0, g)`。g は 0 を含む任意の値(Stage 1 は 0) | must | §6, §18 |
| F-04 | 風: 加速度 `(wind, 0)` を全粒子に加える | must | §18 |
| F-05 | 壁: 粒子の縁が世界の境界 [0,W]×[0,H] を越えたら境界内へ押し戻し、法線方向の速度を `-e·v` にする(e = 反発係数 ∈ [0,1]) | must | §7 |
| F-06 | 粒子同士: 中心距離 < 半径和 なら衝突。法線方向の相対速度を交換する簡略化した弾性衝突(§4.2)。めり込みは半分ずつ押し戻す | must | §8 |
| F-07 | 衝突判定は一様格子(セル幅 = 2×最大半径)で近傍だけを調べる。**総当たりと同じ衝突集合を返す**(G-02) | must | §11, §23 |
| F-08 | 重力井戸 `GravityWell{X,Y,Strength,Radius,Capture,Damping}`: 距離 d < Radius の粒子に、井戸向きの加速度 `Strength / max(d, dMin)²` を与える(§4.3)。Radius 外には効かない。d < Capture では速度が `(1 − Damping·dt)` 倍に減衰する(捕捉) | must | §9 |
| F-09 | 障害物: 軸平行矩形(AABB)。粒子が重なったら最小侵入軸で押し戻し、その軸の速度を `-e·v` にする | must | §19 |
| F-10 | 移動壁: 障害物が時刻 t の関数で往復移動する(`X = X0 + A·sin(ω t)`) | should | §18 |
| F-11 | ブラックホール: 井戸と同じ引力に加え、距離 < EventRadius の粒子を吸収(世界から除く) | should | §18, §20 |
| F-12 | 世界の状態遷移は決定論的: 同じ seed・同じ入力列・同じ dt 列なら同じ状態(G-03) | must | §27 |

### 2.2 ゲーム(`internal/game`)

| ID | 要求 | 優先度 | 原本 |
|---|---|---|---|
| F-20 | 開始時に N 個(既定 500)の粒子を seed 付き乱数で配置する。初速は 0 | must | §16 |
| F-21 | Goal は画面右側の矩形。粒子の中心が入ったら「到達」として世界から除き、到達数を +1 | must | §16 |
| F-22 | 制限時間は 60 秒。0 になったら `timeup`(残り 1ns 未満は 0 とみなす — dt の累積誤差)。到達数が目標(既定 100)に達したら `clear`。clear の判定が timeup より先 | must | §16 |
| F-23 | スコア: 到達 1 個につき +10 × コンボ倍率。**コンボ**は直前の到達から 1.0 秒以内の到達で +1、途切れると 1 に戻る。倍率 = min(combo, 8) | must | §17 |
| F-24 | クリア時のタイムボーナス: 残り秒(切り捨て)× 10 | must | §17 |
| F-25 | 井戸の操作: 追加(上限はステージが決める)/ 移動 / 削除。上限に達した追加は無視する。井戸の既定は Strength 1.0e6・Radius 130・Capture 40・Damping 4(§4.3 の較正) | must | §10 |
| F-26 | Pause / Resume / Reset(同じステージ・同じ seed で最初から) | must | §4, §29 |
| F-27 | ステージ 1〜7 は §5 の表。ステージ切替は Reset を伴う | must | §18 |
| F-28 | 粒子数・重力・風・反発係数はスライダーで変更可。粒子数の変更は Reset を伴う | must | §4, §11 |

### 2.3 Wasm API(`cmd/wasm`)

| ID | 要求 | 優先度 | 原本 |
|---|---|---|---|
| F-30 | `GOOS=js GOARCH=wasm` でビルドした `web/main.wasm` を `wasm_exec.js` 経由で読み込む | must | §14 |
| F-31 | JS → Go: `init(json)` / `step(dt)` / `input(action, x, y, idx)` / `setParam(name, value)` / `bench(n, steps)` | must | §15 |
| F-32 | Go → JS: 粒子位置は **Float32Array(x,y の連続)へのバイトコピー**で渡す(1 フレーム 1 回)。JSON は状態(スコア・時間・井戸・障害物・Goal)だけ | must | §15 |
| F-33 | `step` は物理計算に要した ms を状態に含める(`physicsMs`) | must | §22 |

### 2.4 画面(`web/`)

| ID | 要求 | 優先度 | 原本 |
|---|---|---|---|
| F-40 | Canvas に粒子・Goal・井戸・障害物・ブラックホールを描く。描画は JS のみ | must | §4, §13 |
| F-41 | HUD: Score / Time / 到達数・目標 / Combo | must | §4, §17 |
| F-42 | 右上に FPS / Particles / Physics ms / Render ms | must | §22 |
| F-43 | 左クリック = 井戸追加、ドラッグ = 井戸移動、右クリック = 井戸削除。タッチは左クリック相当 | must | §10 |
| F-44 | スライダー: Gravity / Wind / Particles / Bounce。ボタン: Pause / Reset。ステージ選択 | must | §4 |
| F-45 | Lab Mode: 100 / 500 / 1000 / 2000 / 5000 粒子で `bench` を走らせ、実測の物理時間を表で出す | must | §23, §24 |
| F-46 | フリート共通フッタ(MIT / GitHub / 歩き方 / 設計図 / App Menu・下部固定) | must | フリート規約 |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Go 側は標準ライブラリのみ(`go.mod` に require が無い) | T-002 |
| N-02 | `internal/` は `syscall/js` を import しない(物理と UI の分離。ネイティブで `go test` が走る) | T-003 |
| N-03 | `web/app.js` は粒子の運動則を持たない(数値積分・衝突判定のコードが無い) | 目視 + grep |
| N-04 | 外部 API・保存・ネットワークを使わない。静的配信のみ | 構成 |
| N-05 | 1,000 粒子で物理 1 ステップが実測で報告され、Lab Mode の表に載る | F-45 |

## 4. 設計判断

### 4.1 積分と単位

- 世界座標はピクセル。dt は秒。**クランプは `game.Update` の責務**で dt を [0, 1/20] に丸める(タブ復帰時の大 dt で吹き飛ばない)。`physics.World.Step` はクランプしない(T-101 は dt=1 を使う)。dt=0 の Step は積分せず拘束(衝突・障害物・壁)だけを解く
- 半陰的オイラー(速度を先に更新)を採用。等加速度の解析解と 1 ステップで一致するのは位置でなく速度である
  (位置は `x + v₀·dt + a·dt²` になる)。テストの期待値はこの式で書く

### 4.2 粒子衝突の簡略化

法線 n = (pB − pA)/|pB − pA|。相対速度の法線成分 `vn = (vA − vB)·n` が正(近づいている)のときだけ処理する。

```
j = (1 + e) · vn / (1/mA + 1/mB)
vA -= j/mA · n
vB += j/mB · n
```

e = 反発係数。e = 1 なら運動量・運動エネルギーが保存される(G-01)。
めり込み `pen = rA + rB − d` は両者を `pen/2` ずつ法線方向に離す。

### 4.3 重力井戸の力

`a = Strength / max(d, dMin)²`(dMin = 20px)。逆二乗をそのまま使うと中心付近で発散するので下限を置く。
Radius 外は 0。ステージの井戸上限は §5。井戸は粒子から力を受けない(固定)。

**捕捉減衰(2026-09-07 追加)**: 距離 d < Capture の粒子は速度が毎ステップ `(1 − Damping·dt)` 倍になる。
これで粒子は井戸に落ち着き、**井戸をドラッグすると粒子ごと運べる**。遊び方はこれで決まる ——
「井戸で粒子を捕まえて、Goal まで運ぶ」。

較正(2026-09-07・自動プレイ `playable_test.go`・定点で 6 秒集めて 4〜6 秒で Goal へ運ぶ):
最初に置いた Strength 2e6 / Radius 320 は、床に並んだ粒子を 1 往復で 250〜380 個さらって
10 秒で終わってしまった。**運搬量を決めるのは Strength で、Radius はほとんど効かない**
(0.8e6: 約 50 個/往復、1.0e6: 約 80、1.3e6: 約 125。Radius 90〜160 で同等)。
既定は **Strength 1.0e6・Radius 130・Capture 40・Damping 4**。目標 100 は重力ステージで 1〜2 往復、
風のステージでは左壁に寄った粒子を取りに行って 2 往復になる(G-07)。

### 4.4 データ転送

粒子位置は `[]float32` を `unsafe.Slice` で `[]byte` に見せ、`js.CopyBytesToJS` で JS 側の
`Uint8Array`(= `Float32Array` の buffer)へ 1 回コピーする。5,000 粒子で 40 KB。
JSON を使うのは井戸・障害物・スコアなど数十個の値だけ。

### 4.5 一様格子

セル幅 = 2 × 最大半径。各粒子は自分のセルと隣接 8 セルの粒子と比べる。半径が全て同じなら
これで衝突対の取りこぼしは無い。**格子の結果が総当たりと一致することをテストで確かめる**(G-02)。
粒子対は `i < j` で一度だけ処理する。

ネイティブ実測(2026-09-06・windows/amd64・`go test -bench Step`・960×600・r=3・井戸 1):

| 粒子数 | Step 1 回 |
|---|---|
| 100 | 0.036 ms |
| 500 | 0.069 ms |
| 1,000 | 0.26 ms |
| 2,000 | 0.76 ms |
| 5,000 | 2.18 ms |

Wasm はこの数倍になる見込み。ブラウザでの実測は Lab Mode(F-45)が画面に出す。

## 5. ステージ

| Stage | 名称 | g | 風 | 障害物 | 井戸上限 | 特殊 |
|---|---|---|---|---|---|---|
| 1 | Free Float | 0 | 0 | なし | 1 | 粒子は初速なし。井戸で引き寄せるしかない |
| 2 | Gravity | 300 | 0 | なし | 1 | |
| 3 | Obstacles | 300 | 0 | 矩形 2 枚 | 1 | |
| 4 | Multi Well | 300 | 0 | 矩形 2 枚 | 3 | |
| 5 | Wind | 300 | −80 | 矩形 2 枚 | 3 | 風は Goal と逆向き |
| 6 | Moving Wall | 300 | −80 | 矩形 1 枚 + 移動壁 1 枚 | 3 | |
| 7 | Black Hole | 300 | −80 | 矩形 1 枚 + 移動壁 1 枚 | 3 | ブラックホール 1 個(中央) |

共通: 世界 960×600、粒子半径 3、質量 1、反発係数 0.6、制限 60 秒、目標 100 個、Goal は右端の矩形(x ≥ W−60, y ∈ [H/2−80, H/2+80])。
Stage 1 で粒子が初速なしでは動かない(g = 0)ため、Stage 1 だけ粒子を「画面上半分」に置き、Goal を右端中央に置く(井戸を Goal 付近に置けば集められる)。

## 6. 品質基準(ゲート)

| ID | 内容 | 確認するケース |
|---|---|---|
| G-01 | e = 1 の粒子衝突で運動量ベクトルが保存され、運動エネルギーの相対誤差 < 1e-9 | T-106 |
| G-02 | 格子衝突と総当たり衝突が、ランダム配置 200 粒子 × 20 seed で同じ衝突対集合を返す | T-107 |
| G-03 | 同じ seed・同じ入力列で 600 ステップ後の全粒子位置が bit 一致する | T-108 |
| G-04 | 壁処理後、全粒子が世界の内側にある(縁を含む) | T-104 |
| G-05 | `go vet` 緑・`go test ./...` 緑・`GOOS=js GOARCH=wasm go build` 緑 | 出荷工程 |
| G-06 | 陽性対照: 格子の隣接探索を 4 セルに削った変異体では G-02 が落ちる(テストが衝突を検出できることの証明) | T-107 |
| G-07 | 遊べる保証: 井戸 1 つで「定点で 6 秒集め → 障害物の下を経由して Goal へ運び → 2 秒留まる」を繰り返す機械的な自動プレイで、全 7 ステージが 3 seed とも 60 秒以内に clear。陰性対照: 井戸なしの Stage 1 は到達 0 | T-211, T-212 |

## 7. 原本との差分

| 原本 | 本書 | 理由 |
|---|---|---|
| §15 TypedArray は将来 | F-32 で MVP から採用 | 500 粒子でも JSON 文字列化がフレーム予算を食う。実装コストは小さい |
| §8 「簡略化した弾性衝突」 | §4.2 の式を確定 | テストの期待値を書くには式が要る |
| §9 井戸の力の式が無い | §4.3 で確定 | 同上 |
| §18 Stage 1「重力なし」 | 粒子を上半分に置く | 初速 0 で g = 0 なら何も起きない。井戸で導く遊びは残る |
| §11 上限 5,000 | Lab Mode は 5,000 まで。ゲームのスライダーは 100〜2,000 | 5,000 は物理より描画(fillRect × 5,000)が先に落ちる。実測は Lab Mode で見る |

## 8. スコープ外

原本 §20 / §30 の将来項目: Repulsor・Portal・Magnet・軌跡・発光・衝突エフェクト・サウンド・
ステージエディタ・リプレイ・ランキング・Multiplayer・WebGPU。ゲームパッド。モバイル最適化。
